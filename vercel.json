{
  "version": 2,
  "builds": [
    {
      "src": "backend/server.js",
      "use": "@vercel/node"
    },
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "." }
    }
  ],
  "routes": [
    { "src": "/get-snap-token", "dest": "/backend/server.js" },
    { "src": "/payment-notification", "dest": "/backend/server.js" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}